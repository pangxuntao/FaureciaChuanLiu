spring:
  application:
    name: SigemenBackend
    admin:
      enabled: true
  datasource:
    url: jdbc:mysql://localhost:3306/chuanliu?serverTimeZone=GMT+8&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 880624
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 30
      max-active: 100
      min-idle: 30
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
server:
  port: 8890

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    root: debug
    com.mysql.cj.jdbc.ConnectionImpl: DEBUG
    com.mysql.cj.jdbc.PreparedStatementWrapper: DEBUG
    com.mysql.cj.jdbc.StatementImpl: DEBUG
